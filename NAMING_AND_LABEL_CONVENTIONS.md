# 名称・Gmailラベル命名規則

最終更新日: 2026-07-22
Status: Adopted
Related Project: `google-workspace-personal-work-os`

## 1. 文書の位置付け

本書は、自動期日管理ツールで使用するGoogle Calendar名およびGmailラベル名の正式な命名規則を定める。

本書の内容は、既存のManualモード用ラベル案および`AUTOMATED_DEADLINE_MANAGER_DESIGN.md`に記載された旧ラベル例に優先する。

## 2. 専用Google Calendar

### 2.1 正式名称

```text
自動期日管理
```

### 2.2 用途

- 本ツールが自動登録・更新する重要期限専用のサブカレンダーとする
- タスク管理の正本はGoogle Sheetsとする
- Calendarは、失念時の影響が大きい期限の可視化に限定する
- 軽微な返信、短時間の確認、期限未定タスク、AI推測だけの期限および回答待ちは原則登録しない

### 2.3 説明文案

```text
自動期日管理ツールが登録・更新する重要期限専用カレンダー。タスクの完了・対象外・期限変更はGoogle Sheetsを正本として同期する。
```

## 3. Gmailラベル命名原則

- 先頭の`期日管理/`は使用しない
- 短く、Gmail一覧で判別しやすい名称とする
- AI判定、人間の手動補正、システム状態をルート階層で分ける
- カタカナを使用する場合は半角とする
- 不要な状態ラベルは作成せず、内部管理シートまたはMessage IDで管理する
- 既存Manualモード用ラベル名は新設計へ引き継がない

## 4. 正式Gmailラベル

### 4.1 AI自動付与

```text
AI/要対応
AI/期限
AI/返信待
AI/要確認
```

| ラベル | 意味 |
|---|---|
| `AI/要対応` | 利用者側の対応が必要とAIが判定したメール |
| `AI/期限` | 明示期限または採用可能な相対期限を検出したメール |
| `AI/返信待` | 相手方からの返信・回答待ちと判定したメールまたはスレッド |
| `AI/要確認` | 判定の確信度が低い、期限根拠が曖昧、または人の確認が必要なメール |

AIは上記`AI/`配下のラベルだけを追加・更新する。

### 4.2 人間による補正

```text
手動/取込
手動/除外
```

| ラベル | 意味 |
|---|---|
| `手動/取込` | AIの判定にかかわらずタスク候補として取り込む |
| `手動/除外` | AIの判定にかかわらず自動タスク登録の対象外とする |

人間が付けたラベルはAIが削除しない。`手動/除外`を最優先し、次に`手動/取込`を優先する。

### 4.3 システム異常

```text
SYS/失敗
```

`SYS/失敗`は、AI API、Gmail、SheetsまたはCalendar連携が失敗し、再処理または確認が必要なメールに付与する。

## 5. 作成しないラベル

次の状態はGmailラベルとして作成しない。

```text
判定済み
処理済み
完了
対象外
```

理由は次のとおり。

- AI処理済みかどうかはGmail Message IDを内部管理シートで記録する
- タスクの完了・対象外状態はGoogle Sheetsを正本とする
- Gmailラベルを増やしすぎず、利用者に意味のある状態だけを表示する
- 同一スレッドの新着メッセージを正確に判定するため、スレッドラベルを処理済み判定に使用しない

## 6. ラベル付与の優先順位

```text
手動/除外
  ↓
手動/取込
  ↓
AI/要確認
  ↓
AI/要対応・AI/期限・AI/返信待
```

- `手動/除外`があれば自動登録しない
- `手動/取込`があればAI判定にかかわらず取り込む
- `AI/要確認`は自動確定せず、Sheetsの要確認へ送る
- `AI/要対応`と`AI/期限`は同時付与できる
- `AI/返信待`はタスク状態の更新候補として扱う

## 7. 初期セットアップ

`setupSystem()`は次を自動実行する。

- 専用サブカレンダー`自動期日管理`を検索する
- 存在しなければ作成する
- Calendar IDをUser PropertiesまたはScript Propertiesへ保存する
- 正式Gmailラベルが存在するか確認する
- 不足ラベルだけを作成する
- 旧Manualモード用ラベルは自動削除しない
- 旧ラベルを新処理の判定条件には使用しない

旧ラベルを自動削除しない理由は、既存メールの分類状態や利用者の個別運用へ影響を与えないためである。
