# Prototype v1 Lessons Learned

最終更新日: 2026-07-23  
Status: Adopted as mandatory guardrails for v2  
Related Project: `google-workspace-personal-work-os`

## 1. 文書の目的

本書は、2026-07-23に会社のGoogle Workspace環境で実施したApps Scriptプロトタイプv1.xの導入・検証で発生した問題を記録し、v2再構築で同じ失敗を繰り返さないための強制ルールを定める。

本書の内容は参考情報ではなく、v2の設計・実装・テストで遵守すべき注意事項とする。

## 2. プロトタイプで確認できたこと

- Apps Scriptから、実行・承認したGoogleアカウントのGmail、Sheets、Calendarへアクセスできた
- `setupSystem()`によるシート、Gmailラベル、Calendar、トリガーの生成は技術的に実現可能だった
- `手動/取込`付きGmailスレッドを発見し、Mock Adapterでメールを分類できた
- `2026/8/31`等のスラッシュ区切り・月日1桁の日付もMock期限抽出で認識できた
- Gmail Message IDを処理単位とする方針は妥当だった
- 専用Calendar、Gmailラベル、タスク台帳の連携方針自体は維持できる
- 利用者が日常的に見る画面は1つに集約すべきことが実地検証で確認できた

## 3. 発生した主な問題

### 3.1 初期化前の管理シート参照

`setupSystem()`の最初のステップで、まだ存在しない`system_config`および`history`シートを読み書きし、セットアップが開始できなかった。

再発防止:

- Bootstrap処理は管理シートの存在を前提にしない
- 管理シート作成前のログはConsoleまたはPropertiesの最小情報だけにする
- `getRequiredSheet()`と`getOptionalSheet()`を明確に分ける
- 初期化順序を単体テストする

### 3.2 大量の事前行生成による実行時間超過

11シートに約2,000行分の入力規則、書式、チェックボックス、Protectionを一括設定し、Apps Scriptの最大実行時間を超過した。

再発防止:

- 初期行数は50～100行程度とする
- 行不足時に必要分だけ拡張する
- セットアップは1シートずつ処理し、必要なら継続トリガーで分割する
- 1回のセットアップ処理にはソフト実行時間上限を設ける
- 列単位の大量Protectionを作らない

### 3.3 書込み直後の列マップ再読込

内部列IDを書いた直後に再読込したところ、Apps Scriptの書込みバッファが反映されず、`prompt_versions.prompt_version`が見つからないと判定された。

再発防止:

- 列マップは作成処理内でメモリ上に保持する
- 同じ実行中に書いた内容を即座に再読込して正本扱いしない
- 必要な境界だけ`SpreadsheetApp.flush()`を使用する
- Schema作成とSeed投入を別ステップに分ける

### 3.4 診断処理の肥大化

システム診断が設定・シート・タスクを何度も読み込み、最後にダッシュボード更新まで実行したため、診断だけで最大実行時間を超過した。

再発防止:

- 診断は原則読取専用とする
- 診断中にダッシュボード再計算、行更新、レイアウト修復を行わない
- 各シート・設定は1回だけ読み込む
- Quick DiagnosticとDeep Diagnosticを分ける
- Quick Diagnosticは60秒以内を目標とする

### 3.5 メール処理の過剰走査

手動テストでも最大500スレッド、スレッド内全メッセージ、複数シートの再読込、ダッシュボード更新を実行し、1通のテストでもタイムアウトした。

再発防止:

- 手動テストは最新1メッセージ、最大10スレッドに限定する
- 自動処理開始前は通常Inboxを走査しない
- 1回の実行で設定、Message State、Task indexを1回だけ読み込む
- ラベル検索とMessage処理を分離する
- Worker処理の最後に重いダッシュボード更新を行わない
- 1実行のソフト上限を本番上限より十分短く設定する

### 3.6 チェックボックスのFALSEを実データと誤認

空行へ事前設定したチェックボックスの`FALSE`により、Google Sheetsが空行を使用済み行と判定した。結果として、データが3行目ではなく約2,601行目へ追記された。

再発防止:

- `getLastRow()`を論理データの末尾判定に使用しない
- `task_id`または`origin_key`列を基準に最初の論理空行を探す
- 空行へ値として`FALSE`を事前投入しない
- チェックボックスはData Validationだけを設定し、値は実データ作成時に入れる
- Repositoryの読取時は主キーが空の行を無視する

### 3.7 要確認タブの分離による見逃し

要確認候補を別タブへ入れた結果、利用者がタスク一覧と要確認の2画面を確認する必要が生じた。実データがシート下部へ入った問題も重なり、登録済み候補を見逃しやすかった。

再発防止:

- v2では要確認タブを作らない
- 未確認候補もタスク一覧へ登録する
- 新規候補は`status=REVIEW`、`needs_review=true`とする
- 既存タスクの変更候補は元ステータスを維持し、pending項目へ保存する
- 受入・却下を同じタスク行で行う

### 3.8 VersionとSchema更新の不整合

コード上のVersionを変更しても、旧セットアップ完了フラグが残り、Schema更新を実行せず即時完了した。後付けMigrationも状態管理と競合した。

再発防止:

- v2初期版は既存v1シートとの互換Migrationを持たない
- 新しい空のスプレッドシートへ新規構築する
- `setupSystem()`は新規構築専用とする
- `upgradeSystem()`はv2リリース後のSchema変更だけを対象とする
- Code Version、Schema Version、Applied Migrationを別々に管理する
- Version変更だけでMigration完了と判定しない

### 3.9 物理列位置と入力規則のずれ

旧列位置に残った入力規則・チェックボックスの上へ新しい見出しを追加したため、コメント列にチェックボックス、判断列に`FALSE`が表示された。

再発防止:

- 実行時処理は物理列番号ではなく内部列IDだけを使用する
- Schema変更時は、旧レイアウトへの継ぎ足しではなく新規シートで検証する
- 入力規則・表示形式の型検査をセルフテストへ追加する
- コメント等の文字列列へBoolean Validationが付いていないことをテストする
- Runtime処理から列移動・書式修復を呼ばない

### 3.10 パッチの累積による複雑化

初期化、実行時間、診断、Gmail検索、Review統合、Migration、列正規化を順次パッチした結果、元の前提と現在の前提が混在し、変更の副作用を予測しづらくなった。

再発防止:

- プロトタイプv1.xを本番コードへ昇格しない
- v2は確定した要件を前提にゼロから構築する
- 既存コードのコピー＆パッチを原則禁止する
- Phaseごとに受入テストを通してから次へ進む
- 変更時は仕様書、テスト、コードを同時に更新する

## 4. v2での強制実装原則

1. 日常操作画面は`タスク一覧`へ一本化する
2. 要確認専用タブを作成しない
3. 新規空スプレッドシートだけを初期対象とする
4. v1プロトタイプとの後方互換を初期要件にしない
5. 最初に1本の縦方向フローだけを完成させる
6. `task_id`、`origin_key`、`message_id`を用いた冪等性を最初から実装する
7. 追記位置に`getLastRow()`を使用しない
8. Setup、Runtime、Diagnostic、Migrationの責務を混ぜない
9. メール処理中にレイアウト・書式・入力規則を変更しない
10. 全シート読取を1実行内で繰り返さない
11. すべての長時間処理にソフト実行時間上限を設ける
12. セットアップは最小行数で開始し、必要時だけ拡張する
13. 実データを入れる前にMock Adapterで受入テストを完了する
14. 自動処理開始は明示操作とし、初期値は停止とする
15. AI完了・取消・既存タスク変更は人間確認なしに確定しない

## 5. v1プロトタイプの位置付け

v1.xは、Google Workspace上での技術的実現可能性と運用上の問題を発見するためのプロトタイプとして完了した。

- 本番利用しない
- v2のコードベースとして継ぎ足さない
- 旧スプレッドシートは検証記録として保存してよい
- v2は新しい空のGoogle Sheetsへ構築する
- 必要な旧タスクがある場合だけ、v2安定後に専用移行処理を別途検討する

## 6. v2着手前のゲート

- [ ] 正本4ファイルをv2へ更新
- [ ] v2詳細設計を確定
- [ ] タスク一覧の列・状態・判断フローを確定
- [ ] Setupの最小構成と実行時間予算を確定
- [ ] Gmail手動取込の最小縦フローを確定
- [ ] Mock Adapterの期待出力を確定
- [ ] Phase 1の受入テストを確定
- [ ] 既存v1環境を本番稼働させないことを確認
